{% extends 'base.html.twig' %}

{% block title %}Camion index{% endblock %}

{% block body %}
    <h1>Camion index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Matricule</th>
                <th>Marque</th>
                <th>Couleur</th>
                <th>CapaciteEnKg</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for camion in camions %}
            <tr>
                <td>{{ camion.id }}</td>
                <td>{{ camion.matricule }}</td>
                <td>{{ camion.marque }}</td>
                <td>{{ camion.couleur }}</td>
                <td>{{ camion.capaciteEnKg }}</td>
                <td>
                    <a href="{{ path('camion_show', {'id': camion.id}) }}">show</a>
                    <a href="{{ path('camion_edit', {'id': camion.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('camion_new') }}">Create new</a>
{% endblock %}
