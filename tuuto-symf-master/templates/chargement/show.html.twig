{% extends 'base.html.twig' %}

{% block title %}Chargement{% endblock %}

{% block body %}
    <h1>Chargement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chargement.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ chargement.date ? chargement.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>NumeroChargement</th>
                <td>{{ chargement.numeroChargement }}</td>
            </tr>
            <tr>
                <th>Destination</th>
                <td>{{ chargement.destination }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chargement_index') }}">back to list</a>

    <a href="{{ path('chargement_edit', {'id': chargement.id}) }}">edit</a>

    {{ include('chargement/_delete_form.html.twig') }}
{% endblock %}
