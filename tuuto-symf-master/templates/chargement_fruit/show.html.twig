{% extends 'base.html.twig' %}

{% block title %}ChargementFruit{% endblock %}

{% block body %}
    <h1>ChargementFruit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chargement_fruit.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ chargement_fruit.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chargement_fruit_index') }}">back to list</a>

    <a href="{{ path('chargement_fruit_edit', {'id': chargement_fruit.id}) }}">edit</a>

    {{ include('chargement_fruit/_delete_form.html.twig') }}
{% endblock %}
